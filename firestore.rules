rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to orders for the admin panel, but restrict writes.
    match /orders/{orderId} {
      allow read, list;
      allow write: if request.auth != null;
    }
  }
}
